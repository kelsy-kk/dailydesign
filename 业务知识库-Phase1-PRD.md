# 业务知识库 Phase 1 · 产品需求与交互说明

> **版本**：V1.0  
> **范围**：业务建模 · 知识文档分类、上传登记、文档 ↔ 模型挂接  
> **原型基线**：`业务定义页面还原.html`  
> **周期建议**：2 周（1 个迭代）

---

## 1. 背景与目标

### 1.1 现状

- 业务模型（域模型 / 基础模型）**全部基于元数据**（数仓表、PML 业务对象）搭建。
- 左侧已有「**业务知识库**」目录（业务板块 → 数据域），列表支持 **上传文档**，类型为「文件」。
- 文档当前能力：**上传 → 列表展示 → 预览**，与模型**无结构化关联**，不参与建模与问数。

### 1.2 Phase 1 目标

将「文件」升级为「**知识文档**」，实现：

1. 上传时可**分类登记**（文档类型、摘要、适应场景等）；
2. 文档与域模型 / 基础模型可**双向挂接**；
3. 建模时可**查看关联文档**并对照编辑；
4. 为 Phase 2（字段口径写回）、Phase 3（检索 / AI）预留数据结构与入口。

### 1.3 成功标准

| 指标 | 目标 |
|------|------|
| 文档可分类率 | 新上传文档 100% 填写文档类型与摘要 |
| 挂接覆盖率 | P0 场景域内 P0 文档 ≥ 80% 挂接至少 1 个模型 |
| 建模可发现性 | 模型视图内 1 步可达关联文档预览 |
| 产线可用 | 至少 1 条产线场景完成「上传 → 挂模型 → 建模对照」闭环验证 |

### 1.4 非目标（Phase 1 不做）

- 文档全文解析、切片、向量化
- AI 自动抽取字段 / 口径
- 字段级口径写回（Phase 2）
- 智能问数召回文档（Phase 3）

---

## 2. 用户与场景

| 角色 | 场景 |
|------|------|
| 业务顾问 / 数据顾问 | 上传制度、口径、样表，挂到对应业务对象 |
| 建模人员 | 建域模型时打开关联文档对照字段与口径 |
| 产线实施 | 按数据域整理知识资产，支撑台账 / 问数交付 |
| 智能问数（后续） | Phase 1 仅登记 `scenes` 含「智能问数」，不做召回 |

---

## 3. 信息架构

```
业务板块 (Board)
  └── 数据域 (Domain)
        ├── 域模型
        ├── 基础模型
        └── 知识文档（列表类型仍为「文件」，增强元数据）
              └── N:M ──► 域模型 / 基础模型（ModelDocLink）
```

**列表统一入口不变**：域模型、基础模型、文件同表展示，通过「类型」列区分。

---

## 4. 功能需求

### 4.1 知识文档元数据（P0）

#### 4.1.1 上传登记弹窗

**触发**：点击工具栏「上传文档」并选择文件后，**先弹出登记弹窗**（非直接入列表）。

| 字段 | 控件 | 必填 | 说明 |
|------|------|------|------|
| 文档名称 | 文本，默认取文件名可改 | 是 | |
| 文档类型 | 下拉 | 是 | 定义类 / 规则类 / 样例类 / 参考类 |
| 业务摘要 | 多行文本 | 是 | 1～200 字，写入列表「描述」 |
| 版本 | 文本 | 否 | 如 v1.0 |
| **归属范围** | 单选 | 是 | **平台通用** / **板块通用** / **数据域专用** |
| 归属位置 | 只读 | 是 | 根据归属范围 + 当前树上下文自动展示 |
| 适应场景 | 复选 | 是 | 智能开发 / 智能问数，至少选 1 项 |

**归属范围默认策略**（随左侧树选中节点）：

| 树选中节点 | 默认归属范围 |
|-----------|-------------|
| 全部 | 平台通用 |
| 业务板块 | 板块通用 |
| 数据域 | 数据域专用 |

**确认后**：写入列表一行，类型仍为「文件」；「归属范围」列展示如 `平台通用`、`{板块} / 板块通用`、`{板块} / {数据域}`。

**取消**：不上传、不入列表。

#### 4.1.2 列表增强

在现有列基础上调整：

| 列 | Phase 1 变更 |
|----|-------------|
| 描述 | 展示用户填写的业务摘要 |
| 类型 | 仍为「文件」；**文档类型**独立列展示 |
| **归属范围** | 展示平台 / 板块 / 数据域三级归属 |
| **关联模型** | **新增**，展示「已挂 N 个」，点击可管理 |
| 操作 | 文件行：`预览` `挂接模型` `删除` |

**列表可见性**（选中树节点时）：

| 树节点 | 可见文档 |
|--------|---------|
| 全部 | 全部 |
| 业务板块 | 本平台通用 + 本板块通用 + 本板块下各数据域专用 |
| 数据域 | 平台通用 + 所属板块通用 + 本数据域专用 |

**表头筛选**：「文档类型」下拉（定义类 / 规则类 / 样例类 / 参考类）；「归属范围」文本筛选。

---

### 4.2 文档 ↔ 模型挂接（P0）

#### 4.2.1 从文档挂模型（列表行操作）

**入口**：文件行 →「挂接模型」

**交互**：侧滑抽屉或弹窗

- 标题：`挂接模型 · {文档名}`
- 列表：按文档**归属范围**决定可选模型范围（可搜索）：
  - **数据域专用** → 同 boardId + domain 下模型
  - **板块通用** → 同 boardId 下任意数据域模型
  - **平台通用** → 全平台任意模型
- 每行：模型名称、类型、是否已挂接（勾选）
- 挂接类型（可选）：主依据 / 参考（默认「参考」）
- 备注（可选）：如「第 3 章 字段定义」
- 按钮：确定 / 取消

**确定后**：更新 `relatedModelIds`，刷新「关联模型」列；提示「已挂接 N 个模型」。

#### 4.2.2 从模型挂文档（模型视图）

**入口**：域模型 / 基础模型编辑页 → 顶部栏新增「**关联文档**」按钮（与「切换模型」「保存」并列）

**交互**：右侧抽屉

- 标题：`关联文档 · {模型名}`
- Tab：**本域文档** | **板块通用** | **平台通用** | **已挂接**
  - **本域文档**：当前数据域专用文档
  - **板块通用**：所属板块下板块级通用文档
  - **平台通用**：平台级通用文档
  - **已挂接**：已关联文档列表，可预览、解除挂接
- 每行：文档名、文档类型标签、摘要一行、操作（预览 / 解除）

**空态**：「当前数据域暂无文档，请先上传文档」+ 跳转上传（关闭抽屉并聚焦上传按钮）。

#### 4.2.3 双向一致性

- 从文档侧挂接模型 ↔ 模型侧「已挂接」列表**实时一致**（同一 `ModelDocLink` 数据）。
- 删除文档前：若存在挂接，二次确认「将同时解除与 N 个模型的关联」。
- 删除模型前：同上，解除文档侧关联。

---

### 4.3 文档预览增强（P1）

在现有「文档预览抽屉」底部增加：

- **关联模型**： chips 展示，点击跳转模型视图
- **文档类型 / 版本 / 适应场景**：只读 meta 行

---

### 4.4 左侧目录树（P1）

- 数据域节点计数：**域模型 + 基础模型 + 文件** 合计（已有逻辑扩展即可）
- 选中数据域时，列表标题仍为「{域名} 列表」，含全部类型

---

## 5. 数据模型（研发）

### 5.1 KnowledgeDoc

```typescript
interface KnowledgeDoc {
  id: string;
  name: string;
  description: string;       // 业务摘要
  listType: "file";          // 列表类型固定
  docType: "DEF" | "RULE" | "SAMPLE" | "REF";
  docScope: "platform" | "board" | "domain";  // 归属范围
  boardId: string;   // platform 时为空；board/domain 时必填
  domain: string;    // 仅 domain 范围时必填
  fileUrl: string;
  fileFormat: string;
  fileSize?: number;
  version?: string;
  scenes: ("smart-dev" | "smart-query")[];
  creator: string;
  updatedAt: string;
}
```

### 5.2 ModelDocLink

```typescript
interface ModelDocLink {
  id: string;
  docId: string;
  modelId: string;
  modelType: "domain" | "basic";
  linkType: "primary" | "reference";  // 主依据 | 参考
  remark?: string;
  createdAt: string;
}
```

### 5.3 接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/knowledge/docs/upload` | multipart + 元数据 JSON |
| GET | `/api/knowledge/docs` | `boardId`, `domain`, `docType?`, `scene?` |
| GET | `/api/knowledge/docs/{id}` | 详情含关联模型 |
| PUT | `/api/knowledge/docs/{id}` | 更新摘要、类型、场景等 |
| DELETE | `/api/knowledge/docs/{id}` | 删文档及 links |
| POST | `/api/knowledge/links` | 批量挂接 `{ docId, links: [{ modelId, modelType, linkType, remark }] }` |
| DELETE | `/api/knowledge/links/{id}` | 解除单条 |
| GET | `/api/models/{modelId}/docs` | 模型已挂文档 |

### 5.4 嵌入模式 postMessage（流程原型联动）

```javascript
// 上传完成
{ source: "business-modeling-prototype", type: "business-modeling:upload-doc",
  payload: { docId, name, boardId, domain, docType, scenes } }

// 挂接完成
{ source: "business-modeling-prototype", type: "business-modeling:link-doc",
  payload: { docId, modelId, modelType, linkType } }
```

---

## 6. 交互稿 · 页面级说明

### 6.1 列表页 · 上传文档

```
[上传文档] → 选文件 → ┌─ 登记知识文档 ─────────────────┐
                        │ 文档名称  [成本科目说明_v1.0    ] │
                        │ 文档类型  [规则类 ▼]              │
                        │ 业务摘要  [定义成本科目层级…     ] │
                        │ 版本      [v1.0]                  │
                        │ 所属分类  施工集成 / 台账管理      │
                        │ 适应场景  ☑智能问数 ☐智能开发      │
                        │ 关联模型  [项目成本归集模型 ▼]      │
                        │           [+ 添加]                │
                        │              [取消]  [确认上传]    │
                        └──────────────────────────────────┘
```

### 6.2 列表页 · 文件行

```
| 名称              | 描述           | 类型 | 文档类型 | 所属分类     | 关联模型 | 操作              |
| 成本科目说明.xlsx | 定义成本科目…  | 文件 | 规则类   | 施工/台账   | 2 个     | 预览 挂接 删除   |
```

### 6.3 挂接模型抽屉（从文档进入）

```
┌─ 挂接模型 · 成本科目说明.xlsx ─────────────── × ┐
│ 🔍 搜索模型                                      │
│ ☑ 项目成本归集模型    域模型   主依据 ▼         │
│ ☐ 合同台账对象        基础模型 参考   ▼         │
│ ☐ 材料采购分析模型    域模型   参考   ▼         │
│                              [取消]  [确定]     │
└─────────────────────────────────────────────────┘
```

### 6.4 模型视图 · 关联文档抽屉

```
模型视图顶栏：[‹ 返回] 项目成本归集模型  [关联文档] [切换模型] [保存]

┌─ 关联文档 · 项目成本归集模型 ──────────────── × ┐
│ [ 本域文档 ] [ 已挂接(2) ]                        │
│ ┌────────────────────────────────────────────┐ │
│ │ 成本科目说明.xlsx  规则类  定义成本科目…      │ │
│ │                    [预览] [挂接]              │ │
│ │ 分包管理规范.pdf   规则类  分包结算流程…      │ │
│ │                    [预览] [已挂接 ✓]         │ │
│ └────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

### 6.5 文档预览抽屉（增强）

```
┌─ 成本科目说明.xlsx ─────────────────────────── × ┐
│ 规则类 · v1.0 · 适应：智能问数                    │
│ ┌────────────────────────────────────────────┐ │
│ │            （iframe 预览区）                  │ │
│ └────────────────────────────────────────────┘ │
│ 关联模型：[项目成本归集模型] [合同台账对象]        │
└─────────────────────────────────────────────────┘
```

---

## 7. 业务规则

| 规则 | 说明 |
|------|------|
| R1 | 上传时必须选择**归属范围**；数据域专用需选中具体数据域 |
| R2 | 挂接模型范围：数据域专用→同域；板块通用→同板块；平台通用→全平台 |
| R3 | 同一文档与同一模型仅允许 1 条 link |
| R4 | `primary` 主依据：每个模型建议最多 1 份（超出时 warning，不阻断） |
| R5 | 文件类型扩展名：pdf, doc, docx, xls, xlsx, ppt, pptx, txt, png, jpg, jpeg |
| R6 | 单文件大小上限：50MB（可配置） |

---

## 8. 异常与空态

| 场景 | 处理 |
|------|------|
| 数据域专用但未选数据域 | Toast：「请先选中具体数据域，或改选平台/板块通用」 |
| 登记弹窗必填未填 | 字段下红字，阻断提交 |
| 挂接时无可选模型 | 按归属范围提示「平台/板块/数据域暂无模型」 |
| 预览不支持格式 | 现有 fallback 文案 + 提供下载 |
| 删除已挂接文档 | 确认框说明将解除 N 个模型关联 |

---

## 9. 验收标准（测试用例摘要）

| ID | 用例 | 预期 |
|----|------|------|
| AC-01 | 选文件后弹出登记弹窗，填完确认 | 列表新增文件行，描述为摘要 |
| AC-02 | 登记时不选适应场景 | 阻断并提示 |
| AC-03 | 文件行点「挂接模型」勾选 2 个模型 | 关联模型列显示 2；模型侧已挂接可见 |
| AC-04 | 模型视图点「关联文档」挂 1 份文档 | 文档行关联数 +1 |
| AC-05 | 文档预览底栏点击关联模型 chip | 进入对应模型视图 |
| AC-06 | 删除已挂接文档并确认 | 文档与 links 均删除 |
| AC-07 | 表头按文档类型筛选 | 仅显示对应类型文件 |
| AC-08 | embed 模式上传 | 父页面收到 upload-doc 事件 |
| AC-09 | 上传平台通用文档并挂接跨域模型 | 挂接成功，模型侧「平台通用」Tab 可见 |
| AC-10 | 选中数据域浏览列表 | 可见平台通用 + 板块通用 + 本域专用文档 |

---

## 10. 研发任务拆分

| 序号 | 任务 | 负责 | 估时 |
|------|------|------|------|
| T1 | KnowledgeDoc / ModelDocLink 表结构与 API | 后端 | 3d |
| T2 | 上传登记弹窗 + 列表列扩展 | 前端 | 2d |
| T3 | 挂接模型抽屉（文档侧） | 前端 | 2d |
| T4 | 关联文档抽屉（模型侧） | 前端 | 2d |
| T5 | 预览抽屉增强 + 双向跳转 | 前端 | 1d |
| T6 | 删除级联、权限（同现有列表） | 后端 | 1d |
| T7 | 联调 + 1 条产线场景验证 | 全员 | 2d |

**合计**：约 **10 人日**（1 前端 + 0.5 后端 × 2 周）

---

## 11. Phase 2 / 3 预留

| 字段 / 入口 | Phase 1 | 后续 |
|-------------|---------|------|
| `FieldSemanticSource` | 不展示 | Phase 2 字段设置「口径来源」 |
| `parseStatus` / `KnowledgeChunk` | 不实现 | Phase 3 解析与检索 |
| 文档 `scenes` | 仅登记 | Phase 3 问数召回过滤 |

---

## 12. 附录 · 文档类型速查

| 显示名 | 代码 | 上传建议 |
|--------|------|---------|
| 定义类 | DEF | 对象说明、字段清单 |
| 规则类 | RULE | 制度、口径、计算规则 |
| 样例类 | SAMPLE | 台账 / 报表样表 |
| 参考类 | REF | 方案、白皮书、会议纪要 |

---

*文档维护：与 `业务定义页面还原.html` Phase 1 实现进度同步更新。*
