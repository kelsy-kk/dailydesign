# 业务建模产品原型 · 页面交互说明

本文档用于说明 `业务定义页面还原.html` 原型页面中，用户在不同区域执行操作后会产生什么结果。说明以当前原型实现为准；仅作视觉展示、未绑定逻辑的控件会在文中标注。

---

## 1. 页面整体结构

原型页面自上而下分为四个区域：

| 区域 | 说明 |
|------|------|
| 顶部导航条 | 产品级顶栏（图片形式） |
| 二级 Tab | 物理建模、逻辑建模、**业务建模**（当前高亮）、数据标准 |
| 主工作区 | 左侧「业务知识库」目录树 + 右侧内容区 |
| 浮动入口 | 右下角「AI」按钮（仅展示） |

右侧内容区有两种主视图，互斥显示：

- **列表视图**（`listView`）：业务建模对象列表（域模型、业务对象、文件等）
- **模型视图**（`modelView`）：域模型 / 业务对象的关联配置与字段设置

此外有两类侧滑抽屉，覆盖在页面之上：

- **模型切换抽屉**（`modelDrawer`）
- **文档预览抽屉**（`docPreviewDrawer`）

左侧目录树底部的 **提示条**（`treeTip`）会随操作更新文案，用于反馈当前状态（如选择节点、新增目录、筛选结果等）。

### 嵌入模式

地址栏带参数 `?embed=list` 时进入 **列表嵌入模式**：

- 隐藏顶部导航与二级 Tab
- 收窄左侧目录与表格列宽
- 创建域模型 / 业务对象、保存模型时，向父页面 `postMessage` 发送操作事件（`source: "business-modeling-prototype"`），供外层流程原型联动

---

## 2. 二级 Tab 导航

点击「物理建模」「逻辑建模」「数据标准」等 Tab。

**结果：**

- 当前原型 **未实现** 页面跳转，仅「业务建模」为激活样式
- 属于视觉占位，不改变右侧内容

---

## 3. 左侧 · 业务知识库目录

### 3.1 目录层级

目录为 **两级结构**：

| 级别 | 名称 | 说明 |
|------|------|------|
| 一级 | 业务板块 | 如「PMLead产品」「数据组件」「生产中台」等 |
| 二级 | 数据域 / 产品模块 | 业务板块下为各「数据域」；「PMLead产品」下为产品功能模块 |

**PMLead产品**（一级，默认展开）二级目录包括：

项目信息、市场管理、收入管理、成本管理、机械设备管理、物资管理、劳务分包管理、专业分包管理、资税管理、生产管理、质量管理、技术管理、安全管理。

其余一级节点为原有 **业务板块**，其下二级默认为：支出域、PBS、物资域、收入域、采购域、设备域、风险域、成本域等。

节点名称后括号内数字为 **子项数量或列表条数** 的示意，随树结构或列表行数变化。

### 3.2 展开 / 收起

点击节点左侧 **▸ / ▾** 箭头。

**结果：**

- 仅展开或收起该节点的子级
- 不改变右侧列表内容（除非同时点击了节点名称，见下节）

### 3.3 选择节点

点击节点 **名称**（非箭头、非右侧按钮）。

**结果：**

- 该节点高亮为选中态
- **一级（业务板块）**：更新提示条，例如「已选择：PMLead产品（业务板块）。当前列表级别：数据域。」**不切换**右侧列表标题
- **二级（数据域 / PMLead 模块）**：
  - 右侧切换为列表视图
  - 列表标题变为「{节点名称} 列表」
  - 提示条：`已打开数据域「{名称}」列表，共 N 条。`（PMLead 模块同样走该逻辑，文案仍带「数据域」字样）

### 3.4 搜索目录

在「搜索目录」输入框中输入关键字。

**结果：**

- 实时过滤左侧树：节点名称匹配则保留，父节点因包含匹配子节点也会显示
- 匹配文字高亮
- 提示条显示「正在搜索目录：{关键字}」；清空后恢复默认提示

### 3.5 新增目录

1. 在「创建列表级别」下拉框选择 **业务板块** 或 **数据域**
2. 在输入框填写名称
3. 点击 **新增**（或输入框内按 Enter）

**结果：**

- **新增业务板块**：在树根追加一级节点，并选中；无子级时需后续通过「增」按钮或再次新增数据域
- **新增数据域**：须先选中一个 **一级业务板块**，在其下追加二级节点；未选一级时提示「请先选择一个业务板块，再新增数据域。」
- 名称为空时提示「请先输入要新增的名称。」
- 新增成功后清空输入框并刷新树

### 3.6 节点行内操作（增 / 改 / 删）

每个树节点右侧有三个文字按钮：

| 按钮 | 行为 |
|------|------|
| **增** | 一级节点：在其下新增子级（数据域）；二级节点：新增同级数据域 |
| **改** | 弹出输入框重命名，刷新树并更新提示 |
| **删** | 确认后删除该节点及全部下级；选中态回退到相邻或父节点 |

---

## 4. 右侧 · 业务建模列表

### 4.1 列表标题

默认标题为「业务建模 列表」。选中左侧 **二级目录** 后变为「{目录名} 列表」。

### 4.2 工具栏

#### 所属工程（下拉）

**结果：** 当前为 **视觉展示**，切换选项不改变列表数据。

#### 搜索框（「请输入内容」）

**结果：** 当前为 **视觉展示**，未与列表过滤联动。实际筛选请使用表头列筛选（见 4.3）。

#### 清空筛选

点击 **清空筛选**。

**结果：**

- 清空表头所有列筛选条件
- 清除列排序状态，回到第 1 页
- 提示：「已清空列表筛选条件。」

#### 上传文档

点击 **上传文档**，选择本地文件（支持 pdf、office、图片、文本等）。

**结果：**

- 每选一个文件在列表追加一行，类型为「文件」
- 修改人显示「当前用户」，时间为当前时间
- 操作列为「预览」「删除」
- 刷新分页与左侧树节点计数

#### 新建

点击 **新建** 展开下拉：

- **创建域模型** / **创建业务对象**

**结果：**

- 弹出输入框填写名称；取消则不创建
- 确认后在列表追加一行，并 **自动进入模型视图** 编辑该对象
- 向父页面发送 `business-modeling:create` 消息（嵌入模式下）
- 点击页面其他区域关闭下拉菜单

### 4.3 表头筛选与排序

表格支持按列筛选、排序，并与分页联动。

| 列 | 筛选方式 | 匹配规则 |
|----|----------|----------|
| 名称 | 输入框 | 包含关键字（不区分大小写） |
| 描述 | 输入框 | 包含关键字 |
| 类型 | 下拉 | 精确匹配：域模型 / 业务对象 / 文件 |
| 适应场景 | 下拉 | 行内已勾选场景包含所选值 |
| 修改 | 输入框 | 包含关键字 |
| 修改时间 | 输入框 | 包含关键字 |

点击列标题旁 **↕** 排序按钮：

**结果：**

- 首次点击升序，再次点击降序，按钮显示 ↑ / ↓
- 对当前筛选结果排序后分页展示
- 修改筛选或排序时重置到第 1 页

### 4.4 适应场景（行内复选框）

每行「适应场景」列含 **智能开发**、**智能问数** 两个复选框。

**结果：**

- 勾选状态可切换（原型演示用）
- 变更后重新计算筛选与分页（若表头场景筛选已启用）

新建行时默认勾选规则：

- 域模型：默认勾选「智能问数」
- 业务对象：默认勾选「智能开发」
- 文件：两项默认均勾选

### 4.5 列表行操作

| 操作 | 类型 | 结果 |
|------|------|------|
| 点击 **名称** | 域模型 / 业务对象 | 进入 **模型视图**，标题为该行名称 |
| 点击 **名称** / **预览** | 文件 | 打开 **文档预览抽屉** |
| **详情** | 域模型 / 业务对象 | 同点击名称，进入模型视图 |
| **删除** | 全部 | 移除该行，刷新分页与树计数，并提示 |

列表初始化时会注入一批演示数据（域模型、业务对象、文件混合）。

### 4.6 分页

底部分页显示：`共 N 条，每页 15 条`。

**结果：**

- **上一页 / 下一页**：在筛选结果内翻页
- 页码按钮：跳转对应页；超出总页数的页码按钮隐藏
- 筛选后条数变化时，当前页自动收敛到有效范围

---

## 5. 模型视图（域模型 / 业务对象）

从列表进入后，列表隐藏，展示模型编辑界面。

### 5.1 顶部栏

| 控件 | 结果 |
|------|------|
| **‹ 返回** | 回到列表视图，不删除已编辑内容 |
| 标题 | 显示当前模型 / 业务对象名称 |
| ✎ | **视觉展示**，未绑定重命名 |
| **切换模型** | 打开右侧 **模型切换抽屉** |
| **保存** | 提示「已保存{类型}：{名称}」；嵌入模式下向父页面发送 `business-modeling:save` |

### 5.2 左侧资源区

包含「数据类型」下拉、表/视图搜索框、资源树。

**结果：** 当前均为 **静态示意**，拖拽、筛选未实现业务逻辑。

### 5.3 关联配置 / 字段设置 Tab

点击 **关联配置** 或 **字段设置**。

**结果：**

- 切换 Tab 激活样式与对应面板
- **关联配置**：空状态插画 + 文案「请将左侧数据资源拖拽至右侧面板」（拖拽未实现）
- **字段设置**：展示示例字段表（多组「测试0001」等），语义类型下拉、开关为静态 UI

---

## 6. 抽屉

### 6.1 模型切换抽屉

由模型视图「切换模型」打开。

**结果：**

- 展示模型下拉、编辑按钮、Tab、示意性关联图（卡片与 Join 连线）
- **关闭（×）** 或 **关闭按钮** 隐藏抽屉
- 抽屉内 Tab、编辑、画布元素：**视觉展示**，无完整交互

### 6.2 文档预览抽屉

由文件行的预览或文件名链接触发。

**结果：**

- 标题为文件名，iframe 加载文件 URL（上传文件为 blob 地址，演示文件为空白页）
- 显示预览说明文案
- 点击 **×** 关闭并清空 iframe

---

## 7. 其他说明

### 7.1 仅视觉展示的元素

- 顶部产品 Tab（除业务建模外）
- 工具栏「所属工程」、全局搜索框
- 模型视图标题旁 ✎、资源区筛选与资源树交互
- 模型切换抽屉内编辑与画布
- 右下角「AI」浮动按钮
- 关联配置区的拖放建模

### 7.2 与 UI 素材的关系

`UI素材/可折叠筛选面板-业务板块数据域.html` 为独立的 **业务分类 + 二次筛选** 组件 demo（可折叠面板、业务板块/数据域多选、收藏/模版/协作筛选），**尚未合并** 进本页面。若后续集成，可在列表工具栏与表格之间增加对应筛选区，并在本文档中补充章节。

### 7.3 文档维护

原型文件变更后，请同步更新本文档。以 `业务定义页面还原.html` 内脚本行为为准。

---

*文档版本：与当前 `业务定义页面还原.html`（含 PMLead产品 目录树）一致。*
